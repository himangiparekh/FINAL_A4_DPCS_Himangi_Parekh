
#ifndef CONSTANTS_H
#define CONSTANTS_H

#include <iostream>
#include <vector>
#include <cmath>
#include <cstring>
#include <SDL2/SDL.h>
#include <Eigen/Dense>
#include <map>
#include <algorithm> 
#include <limits>
#include <utility>
#include <cstdio>
#include <string>

// Constants for SDL Window dimensions
const int WINDOW_WIDTH = 800;
const int WINDOW_HEIGHT = 600;
const double EPSILON = 1e-9;
const int SCREEN_WIDTH = 800;
const int SCREEN_HEIGHT = 600;
const float ISO_ANGLE = M_PI / 6; // 30 degrees
const float SCALE_FACTOR = 50.0f;


#endif